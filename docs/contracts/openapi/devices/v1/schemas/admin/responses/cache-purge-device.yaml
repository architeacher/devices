description: Device cache purged successfully
content:
  application/json:
    schema:
      $ref: "entities/cache.yaml#/CachePurge"
    examples:
      purged:
        $ref: "../examples/cache.yaml#/purge_single_device"
