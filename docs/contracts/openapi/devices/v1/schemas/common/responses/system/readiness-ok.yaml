description: Service is ready to accept traffic
content:
  application/json:
    schema:
      $ref: "entities/readiness.yaml#/Readiness"
    examples:
      ok:
        $ref: "examples/readiness.yaml#/ok"
